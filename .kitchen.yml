---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[ec-tools::usage]
    attributes:
      # chef:
      #   config:
      #     chef_server_root: 'https://chef_server_root'
